// eslint-disable-next-line no-restricted-imports
import { DefaultTheme } from 'styled-components';

export type ThemeType = 'default' | 'white' | 'outline' | 'darker';

export const defaultTheme: DefaultTheme = {
  type: 'default',
};
